export IS_WSL=$(uname -a | grep -i -c "microsoft")

if [[ "${IS_WSL}" -eq 1 ]]; then
    # start systemd genie
    # disabled as we don't really need to run in a shell with systemd and it loses the CWD
    # exec /usr/bin/genie -l
fi
